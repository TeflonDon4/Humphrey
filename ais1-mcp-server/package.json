{
  "name": "ais1-mcp-server",
  "version": "1.0.0",
  "description": "MCP server and CLI for the AIS-1 Agent Identity Standard registry",
  "main": "dist/server.js",
  "bin": {
    "ais1": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "cli": "ts-node src/cli.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "chalk": "^4.1.2",
    "commander": "^12.0.0",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": ["ais1", "mcp", "ai-identity", "agent-identity"],
  "license": "CC0-1.0"
}
